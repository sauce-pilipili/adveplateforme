{% extends 'baseAdmin.html.twig' %}
{% block title %}Utilisateur{% endblock %}
    {% block body %}
        <div class="main">
            {{ include('_include/_navigation.html.twig') }}
            <div class="content">
                <div class="welcome-container-title">
                    <h1 style="flex: none">Ajout d'un membre</h1>
                    <a class="bouton-2" href="{{ path('user_index') }}">Quitter <i class="fas fa-times"></i></a>
                </div>
                <div class="welcome-container form">
                    <div class="formCategorie">
                        {{ include('user/_form.html.twig') }}
{#                    {{ form_start(form) }}#}
{#                    <p>Ajouter une adresse mail</p>#}
{#                    {{ form_row(form.email) }}#}
{#                    <p>Ajouter un niveau d'accès</p>#}
{#                    {{ form_row(form.roles) }}#}
{#                    <p>Ajouter un mot de passe</p>#}
{#                    {{ form_row(form.password) }}#}
{#                    <button class="bouton-1" style="border: none">{{ button_label|default('Enregistrer') }}</button>#}
{#                    {{ form_end(form) }}#}
                </div>
            </div>
            </form>
        </div>
        </div>
    {% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/formulaire.css') }}"
{% endblock %}
