{% extends 'baseAdmin.html.twig' %}
{% block title %}Utilisateur{% endblock %}
    {% block body %}
<div class="main">
    {{ include('_include/_navigation.html.twig') }}
    <div class="content">
        <div class="welcome-container-title">
            <h1 style="flex: none">Modifier l'utilisateur</h1>
            <a class="bouton-2" href="{{ path('user_index') }}">Quitter <i class="fas fa-times"></i></a>
        </div>
        <div class="welcome-container form">
            <div class="formCategorie">
    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>

    </div>
</div>
    {% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/formulaire.css') }}">
{% endblock %}
