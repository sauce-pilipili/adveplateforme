{% for article in articles %}
    <tr>
        <td><a style="cursor:pointer " href="{{ path('articles_edit', {'id': article.id}) }}">
                {{ article.title }}
            </a></td>
        <td>{{ article.createdDate ? article.createdDate|format_datetime(locale='fr',pattern="EEEE dd MMMM YYYY") : '' }}</td>
        <td>
            {{ include('articles/_delete_form.html.twig') }}
        </td>
    </tr>
{% else %}
    <tr>
        <td colspan="17">Aucun résultat</td>
    </tr>
{% endfor %}