{% extends 'base.html.twig' %}

{% block title %}CentresCategories{% endblock %}

{% block body %}
    <h1>CentresCategories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ centres_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ centres_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('centres_categories_index') }}">back to list</a>

    <a href="{{ path('centres_categories_edit', {'id': centres_category.id}) }}">edit</a>

    {{ include('centres_categories/_delete_form.html.twig') }}
{% endblock %}
