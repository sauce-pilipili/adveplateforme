{% extends 'baseAdmin.html.twig' %}
{% block title %}Centres{% endblock %}
    {% block body %}
        <div class="main">
            {{ include('_include/_navigation.html.twig') }}
            <div class="content">
                <div class="welcome-container-title">
                    <h1>Ajout d'un centre dans {{ cat }}</h1>
                    <a class="bouton-2" href="{{ path('centres_index',{name: cat}) }}">Quitter <i class="fas fa-times"></i></a>
                </div>
                <div class="welcome-container">
                    {{ include('centres/_form.html.twig') }}
                </div>
            </div>
        </div>
    {% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/formulaire.css') }}">
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('js/imagePreview.js') }}"></script>
{% endblock %}