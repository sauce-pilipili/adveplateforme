<form method="post" action="{{ path('classe_decouverte_categorie_delete', {'id': classe_decouverte_categorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classe_decouverte_categorie.id) }}">
    <button class="btn">Delete</button>
</form>
